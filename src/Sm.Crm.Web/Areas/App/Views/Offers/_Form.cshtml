
@model CreateOrEditOfferDto

<form asp-action="@(Model.OfferStatusId != null && Model.OfferStatusId > 0 ? "Edit" : "Add")" role="form" method="post">
    @await Html.PartialAsync("_ModalHeader", new ModalHeader { Heading = (Model.OfferStatusId != null && Model.OfferStatusId > 0 ? "Edit" : "Add") + " Request Status" })
    <div class="modal-body form-horizontal">
        @if (!Html.ViewData.ModelState.IsValid)
        {
            var error = "An error occured";
            <partial name="_Alert" model="error" />
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
        }
        <input type="hidden" asp-for="OfferStatusId" />
        <div class="row mb-3">
            <label asp-for="OfferStatusId" class="col-lg-3 col-form-label"></label>
            <div class="col-lg-9">
                <input asp-for="OfferStatusId" class="form-control" />
                <span asp-validation-for="OfferStatusId" class="text-danger"></span>
            </div>
        </div>
       
    </div>
    @await Html.PartialAsync("_ModalFooter", new ModalFooter { SubmitButtonText = Model.OfferStatusId != null && Model.OfferStatusId > 0 ? "Update" : "Save" })
</form>

@{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
